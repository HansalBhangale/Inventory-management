# Copy this file to ".env" (same folder) and fill in your key. .env is gitignored — never commit it.
# Used only by the optional M7 "Why?" copilot to polish explanations with Gemini.
# Without it, the copilot still works using the built-in rule-based explanation.

# Get a key at https://aistudio.google.com/app/apikey  and: pip install google-generativeai
GOOGLE_API_KEY=
